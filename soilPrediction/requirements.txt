# ============================================================
# SmartAgro — Unified App  |  requirements.txt
# Install all at once:  pip install -r requirements.txt
# ============================================================

# Gradio interface (must come first — dictates fastapi version)
gradio==6.9.0

# Web framework — version must satisfy gradio 6.9.0 constraint (>=0.115.2)
fastapi>=0.115.2            # REST API + static file serving
uvicorn>=0.29.0             # ASGI server to run FastAPI
python-multipart>=0.0.9     # Required for file uploads (UploadFile)

# TensorFlow for loading soil_model.h5
tensorflow==2.15.0

# Image processing
Pillow==10.2.0              # Open and resize images
numpy==1.26.4               # Array math

# API Integration
requests==2.31.0            # HTTP requests for Hugging Face API
python-dotenv==1.0.0        # Load API keys from .env file

# Dataset download helper (optional)
kaggle==1.6.6               # Download datasets from Kaggle CLI

# Model performance reporting (used in train_model.py)
scikit-learn==1.4.1.post1   # Confusion matrix, classification report
matplotlib==3.8.3           # Plot training graphs
seaborn==0.13.2             # Pretty confusion matrix heatmap